# copy this file to .env and fill values
DJANGO_SECRET_KEY=replace-with-a-secure-key
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DATABASE_URL=postgres://django:password123@db:5432/beauty

# Firebase admin credential: either a path to a service account JSON file
# or the raw JSON string. Example (path): /secrets/firebase-sa.json
FIREBASE_ADMIN_CREDENTIAL=
